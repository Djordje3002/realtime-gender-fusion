# Pinned deliberately:
#   albumentations 2.x renamed the arguments used in the training scripts
#   (ImageCompression quality_lower -> quality_range, CoarseDropout max_holes ->
#   num_holes_range). Staying on 1.4.x keeps the scripts as written.
#   torch 2.4+ is required for the torch.amp.autocast(device_str) form.

torch>=2.4
torchvision>=0.19
timm>=1.0.7
albumentations>=1.4,<2.0
opencv-python>=4.9
mediapipe>=0.10.14
ultralytics>=8.2
scipy>=1.11
numpy>=1.26
pillow>=10.0
